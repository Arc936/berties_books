<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bargain Books (Under £20)</title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
</head>
<body>
    <div class="container">
        <h1>Bargain Books (Under £20)!</h1>

        <% if (books.length > 0) { %>
            <ul>
                <% books.forEach(book => { %>
                    <li>
                        <span class="book-title"><%= book.name %></span>
                        <span class="book-price">£<%= parseFloat(book.price).toFixed(2) %></span>
                    </li>
                <% }) %>
            </ul>
        <% } else { %>
            <p style="text-align: center; font-style: italic;">Sorry, no books are currently priced under £20. Check back later!</p>
        <% } %>

        <div class="back-link">
            <a href="/">Back to All Books</a>
        </div>
    </div>
</body>
</html>